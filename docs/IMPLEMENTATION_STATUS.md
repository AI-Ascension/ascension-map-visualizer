# Implementation status

Status: implemented components under integration; the complete host/provider feature is not yet accepted.

The repository was created privately under the assignment's fallback visibility rule. Work is on `feat/complete-map-visibility` above the initial `bootstrap` default branch. No merge, release, deployment, or service installation has been performed. Three disposable game launches terminated before any model decision or game action. Companion changes use isolated worktrees; unrelated shared changes remain preserved.

## Product evidence

The Rust product implements strict bundle validation, deterministic SVG/PNG rendering, bounded loopback serving, historical replay, optional OTLP export, and an embedded offline demo. The browser implements read-only inspection, search, overlays, route comparison, and replay controls. CLI tests exercise the actual executable from an unrelated working directory.

The dense harness fixture at `3c4470f0940e3668519194e13b59f234e75821a7` contains 76 nodes and 182 edges. Clean source commit `5b1d196480685a313bc2417d5b4450a63cc89ce5` was built and packaged natively on Linux and Windows. Both binaries passed local-prefix and independent home-path scans in UTF-8 and UTF-16. The earlier `0fb4670` archives contained embedded local build paths and are superseded. Both new archives were extracted, checksum-verified, and used to run `doctor`, reproduce the offline demo, and validate it. Independent [packaging review](evidence/release-privacy-review.md) repeated archive scans and native smoke tests and confirmed the old archive fails the same privacy scan. All ten generated files were byte-identical across platforms, including PNG, SVG, and browser assets; see [native comparison](evidence/native-demo-comparison.json) and [packaging evidence](evidence/native-packaging.json).

The updated source passed all 31 Linux and 29 Windows Rust tests, formatting, and Clippy. Unix-only path/symlink tests explain the platform count difference. Dependency advisories/licenses/sources passed after the keyed telemetry repair. Browser commit `04d6d00` passed all 18 desktop/mobile tests with fresh actual-CLI bundles; missing generated inputs fail CI. Hosted CI [34092979262](https://github.com/AI-Ascension/ascension-map-visualizer/actions/runs/34092979262) passed all five jobs at `5b1d196`, including native packaging, all 31/29 Rust tests, and all 18 browser tests. The subsequent documentation commit `c29d8a8b6599ba1eca847f2ef23135edbb32c394` also passed all five hosted jobs in [34132236336](https://github.com/AI-Ascension/ascension-map-visualizer/actions/runs/34132236336). Product PR [#2](https://github.com/AI-Ascension/ascension-map-visualizer/pull/2) remains a draft.

## Companion integration

The accepted neutral protocol dependency is pinned to `d9ffb190ad8990e15f43d7992581dcb2d60b1971`. Copied harness contracts and fixture provenance are machine-checked by `tools/contracts.mjs`.

Host addon source `f62fedbc28eef4f15f4b41a0d45da5999edc0c84` integrates current main packaging changes and bounds fingerprint child enumeration before retaining another edge. Root independently reviewed that repair, passed the managed map probe, and verified all three rebuilt addon hashes before staging them in the disposable host. Owner validation covers exact-host compilation, Rust and policy gates, and managed source-only probes. The Windows Workshop probe is blocked by unprivileged symlink creation; the Linux owner probe passed. Host [PR #54](https://github.com/AI-Ascension/sts2-game-mod/pull/54) passed all three checks at that addon source. Its newly pushed head `089a60dc9f6377cc38b23cfab3b12ad0a00aeef7` adds launcher/trace validation only; all three hosted checks passed at this exact head. No addon source changed. The three disposable live attempts are described below. Gateway is at `4b306321f869f0dffb99110cc127dcdac731df4a`. MCP authority repair `0d2035a08cbab9210e89ee0c1adc560fcf47d327` passed independent pre-forward fencing and zero-forwarding regressions. Protocol [#18](https://github.com/AI-Ascension/sts2-protocol/pull/18), gateway [#19](https://github.com/AI-Ascension/sts2-gateway/pull/19), and MCP [#24](https://github.com/AI-Ascension/sts2-mcp-server/pull/24) are drafts with passing checks at these pinned heads.

Harness assembly at `c4574555bf383014479b5651b59e0f90c0a40715` passed formatting, full workspace Clippy, all 249 tests across 38 suites, and strict policy (433 files, zero warnings/errors). The independent graph oracle passed all nine tests. The measured synthetic evaluation passed all four modes using the pinned product renderer. Production topology/navigation, analysis, and render caches passed independent review after repairs to fresh catalog/payload validation, bounded reads, duplicate-key rejection and telemetry. Store publication uses a bounded operating-system lock and finite retention. These checks establish source behavior, not live host settlement. Harness [PR #35](https://github.com/AI-Ascension/sts2-harness/pull/35) was published as a draft at this exact commit and passed both hosted checks. The later diagnostic head `4e8c93a3b1fc7cb3ca5ddab3476c0290cf89f9eb` passed all 257 workspace tests, full Clippy/format checks and strict policy (435 files, zero warnings/errors), and was pushed to the same draft PR. The two-file request diagnostic was independently reviewed and committed unchanged after validation.

The [actual Astra CLI capture](evidence/provider-cli-capture.json) was repeated against the rebuilt bridge at `99ee3eac080ac03a186e0081505f131ea95d3a3c`; its binary remains byte-identical after the latest request-diagnostic change. It passed with 76 nodes, 182 edges, three legal bindings and a byte-identical 253586-byte renderer PNG. It runs the real bridge executable with a private fake provider CLI and verifies the emitted prompt, image bytes, execution identity and bounded legal decision. This is synthetic boundary evidence. Real provider graph/image delivery and a host-settled map action remain unverified.

Three bounded disposable attempts terminated with zero provider decisions and zero game actions. The first failed before readiness because Steam was not open. After the user opened Steam, the second reached host readiness but failed on the first episode observation. Its trajectory was empty. The third attempt used the first diagnostic repair, but still reported a generic MCP request failure. A second reviewed repair now distinguishes fixed transport categories, including timeout, envelope and correlation failures. The fourth launch was withheld because Steam had closed. Steam preflight detection is tracked and independently triaged in [host issue #56](https://github.com/AI-Ascension/sts2-game-mod/issues/56). All three attempts ran cleanup, and all 683 protected files and their backups were rehashed afterward with zero mismatches or additions.

The campaign acceptance guard permits at most two model decisions, requires settled `start_run` then `select_map_node` actions, and captures fresh read-only context before releasing authority. Launcher trace validation checks ordered model/action/operation/state/image/artifact correspondence and rejects unaccounted operations or trailing events. All 22 trace checks pass at mod commit `089a60dc9f6377cc38b23cfab3b12ad0a00aeef7`; strict policy reports zero warnings/errors. Independent guard review closed all findings and passed the three campaign tests twice from a clean harness archive at `2ab45da`. The disposable host remains stopped, and the 683 protected source files and their backups still match with no additions.

The documentation-only observability companion is [PR #12](https://github.com/AI-Ascension/ai-agent-observability/pull/12) at `67f455c0d4aa142139b3942ed0ad56138eecbf8b`. Shell syntax, ShellCheck, fixture/bootstrap tests, native Windows Compose configuration and invariant checks passed. Hosted CI also passed its deployment-contract and Dockerfile validation gate. No Collector service was started and no backend ingestion is claimed.

## Orchestration evidence

Installed client `0.153.4` accepted explicit `gpt-5.6-luna` / `max` settings for eight original descendants, four resumed tasks and two Steam issue/triage tasks. Independent runtime metadata confirmed that pair for all fourteen, each directly under the root at depth one. The original agents were absent from the native registry before resumed tasks were assigned. The root remained `gpt-6-astra`. The peak of eight descendants stayed below the project ceiling of twelve.

The mandatory three descendant layers are blocked: both original and resumed child tool surfaces expose no delegation operation. No flat tree is represented as depth three, and no alternative clients or independent roots were used to bypass the limitation. Supported project keys were parsed; explicit spawn arguments are verified, while no effective project-config reload is claimed. See [sanitized orchestration evidence](orchestration-observed.json).

The complete acceptance gates and remaining work are recorded in [acceptance.json](acceptance.json) and the [requirement-to-test matrix](REQUIREMENTS.md). Component tests, synthetic captures, and successful packaging do not establish the live vertical slice.

## Historical replay repair

Harness `423d9052c8780173e640fa5991e88e8bae282f47` retains the exact validated source snapshot, analysis/version, manifest lineage, and every historical source binding independently of candidate pruning. Historical bindings cannot become dispatchable through the replay API. The focused regression and all 259 workspace tests passed, as did formatting, full Clippy, strict policy (436 files, zero warnings/errors), and runtime/bridge builds. Source hashes remained unchanged during those checks. Independent replay review stopped at an account usage limit and is incomplete. The rebuilt bridge passed a fresh synthetic CLI capture: 76 nodes, 182 edges, three bindings, and byte-identical rendered/attached PNG. The runtime has not been launched, and no real provider was called. Steam remains unavailable.
