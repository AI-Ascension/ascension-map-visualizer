# Planning-stage standards profile

The default bootstrap revision contains an orchestration prompt, AGENTS.md,
license and agent configuration. It has no accepted Rust workspace, browser
product or executable test target. Documentation and configuration are the
active standards scope. Rust and browser runtime lanes are not applicable to
this revision, even when implementation exists on a separate branch.

The local profile/lock pins AI-Ascension/.github standards data. AGENTS.md retains
the owner boundary: the visualizer consumes public bounded map data, protocol
owns contracts, and harness owns analysis. No standards adoption initializes a
product workspace or grants direct host authority. UTF-8/LF is the source default.

Check local links, parsed configuration and Git whitespace with the local command
in standards-profile.toml. The prompt's client configuration example is a proposed
input, not proof that the installed runtime accepts those keys or an agent uses
the stated model. This standards run observed codex-cli 0.153.4 with native
model/effort spawn arguments; actual execution metadata must establish identity
and ancestry. `codex --strict-config features list` is unsupported in that client
and is not a successful configuration check.

When product code is accepted, update the profile and execute actual pinned
build, lint, tests and browser checks. Missing or zero targets cannot pass an
active runtime lane. Preserve the implementation prompt as an assignment record;
it cannot override a newer operator's narrower authorization.
